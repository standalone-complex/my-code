#include <stdio.h>
#include <stdlib.h>

void yanghui(int);

int main()
{
    int n, i, j;
    scanf("%d", &n);
    yanghui(n);
    return 0;
}

void yanghui(int n)
{
    int i, j;
    int** a = (int**)malloc(sizeof(int*) * n);
    for(i=0; i<n; i++)
    {
        
    }
}